[tool.poetry]
name = "src"
version = "0.1.0"
description = ""
authors = ["sofiaaair <sofia.airas@hotmail.com>"]

[tool.poetry.dependencies]
python = "^3.6.9"
toml = "^0.10.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
